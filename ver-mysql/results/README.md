# Results Folder

This folder is reserved for actual MySQL query outputs.

Current environment note: the local `mysql` and `mysqladmin` commands were not available during this run, so query result files were not generated here. Run `scripts/check.sh` in an environment with MySQL available to execute the SQL files and create result artifacts.
